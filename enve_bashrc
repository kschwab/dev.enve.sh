# Enve run command and environment settings

# Exports #
###########
export PS1="\[\e[1;30;42m\]📦ENVE\[\e[1;49;32m\] \[\e[0;1;32m\]$ENVE_ID $ENVE_ID_VER\[\e[24m\]\n[\[\e[0m\]\u@\h \W\[\e[1;32m\]]\[\e[0m\]$ "
if [[ $ENVE_PATH ]] && [[ ":$PATH:" != *":$ENVE_PATH:"* ]]; then export PATH=$ENVE_PATH${PATH:+:${PATH}}; fi

# Aliases #
###########
alias ls='ls --color=auto'
