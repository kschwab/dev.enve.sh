# Enve run command and environment settings

# Exports #
###########
export PS1="\[\e[1;32m\]ENVE 📦\[\e[4m\]$ENVE_ID\[\e[24m\]\n[\[\e[0m\]\u@\h \W\[\e[1;32m\]]\[\e[0m\]$ "
if [[ $ENVE_PATH ]] && [[ ":$PATH:" != *":$ENVE_PATH:"* ]]; then export PATH=$ENVE_PATH${PATH:+:${PATH}}; fi
if [[ ":$PATH:" != *":/usr/lib/sdk/enve/bin:"* ]]; then export PATH=/usr/lib/sdk/enve/bin${PATH:+:${PATH}}; fi

# Aliases #
###########
alias ls='ls --color=auto'
